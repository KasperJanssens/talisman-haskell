module DungeonMasterTest where

{-import Test.Hspec
import qualified DungeonMaster as DungeonMaster

spec::Spec
spec = describe "Dungeon master" $ do
        it "should be able to put players on the board" $ do
            runState (do
                         let ogre = OgreChieftain ogreChieftain
                         place ogre) spaces-}
            
